# Archived plans — design rationale for shipped work

Plans in this directory describe **completed** work. They live under version control because the design-rationale paragraphs (alternatives considered, why-not-X arguments, contemporary deliberation) aren't preserved in CLAUDE.md's compressed summary or in commit messages. Read them when you want to understand *why* a shipped design choice was made, not *what* the design is — the latter lives in CLAUDE.md and the code.

| File | Topic | Shipped at |
|---|---|---|
| [2026-04-29_sequencing-module-outline.md](2026-04-29_sequencing-module-outline.md) | Initial `constellation.sequencing` module layout + stub session — established the no-Library/Quant/Search-trio rule, `Samples` first-class, pure-functional core + `Project` orchestrator, `align/` verb-not-backend naming, `ragged_trace_1d` lifted to `core.io`, `signal/models/` ABC for future physics-based basecallers. | Foundational; subsequent commits filled in stubs. |
| [2026-05-02_s1-demux-port.md](2026-05-02_s1-demux-port.md) | NanoporeAnalysis transcriptomics pipeline → `constellation transcriptome demultiplex` CLI port. Includes the `_fixed1` parity baseline, `Scorer` Protocol seam, segmented-edlib-over-Smith-Waterman rationale, the three deliberate `HardThresholdScorer` bug corrections vs upstream. | `1817255` (S1 ship), `e85d019` (simulator + BAM reader), `cd89604` (streaming ingest), `21e758b` (per-worker file chunks), `bc0b754` (trimmed-window streaming). |
| [2026-05-03_s1-reference-layer.md](2026-05-03_s1-reference-layer.md) | Three-sibling reference container split: `GenomeReference` + `Annotation` + `TranscriptReference` + `GeneticTools`. Hand-rolled stdlib-only FASTA/GFF3/BED parsers (the no-pyranges argument). `feature_quant` multi-tag with `feature_origin` column. `constellation reference {import,fetch,summary,validate}` CLI. | `c77d5c9`. |
| [2026-05-03_s2-mode-a-genome-counting.md](2026-05-03_s2-mode-a-genome-counting.md) | S2 Mode A `transcriptome align`: minimap2 splice-aware alignment + `Alignments` container + filter step + gene-level overlap counter. The two-layer minimap2 split (`minimap2_run` generic runner + `map_to_genome` orchestrator). Streaming-pipeline rule + memory budget. | `bfdce88` (Mode A ship), `b82ba6c` (gene × sample TSV + TPM), `8a9af05` (count-mode int64 dtype fix), `4ed4638` (Phase 1 alignment-derived intermediates from `docs/plans/de-novo-clustering-roadmap.md`). |

The active sequencing roadmap is [../de-novo-clustering-roadmap.md](../de-novo-clustering-roadmap.md). Phase 1 of that roadmap also ships at `4ed4638`; Phases 2–4 are pending.
