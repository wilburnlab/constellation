"""NN architectures assembled from core.nn primitives.

Domain-agnostic models live here; domain modules may reference them but
the model definitions themselves depend only on core.

Modules (TODO): chronologer, peptide_encoder, spectrum_predictor, collage.
"""
